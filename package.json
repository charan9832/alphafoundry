{
  "name": "alphafoundry",
  "version": "0.2.0",
  "description": "AlphaFoundry: an opencode-style terminal TUI powered by Pi Agent.",
  "type": "module",
  "bin": {
    "af": "./src/cli.js",
    "alphafoundry": "./src/cli.js"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "npm test && node src/cli.js --help && node src/cli.js --version",
    "pack:dry-run": "npm pack --dry-run --json"
  },
  "dependencies": {
    "@mariozechner/pi-coding-agent": "^0.70.6"
  },
  "engines": {
    "node": ">=20.6.0"
  },
  "files": [
    "src",
    "test",
    "README.md",
    "AGENTS.md",
    "docs"
  ]
}
